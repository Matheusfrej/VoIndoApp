import styled from 'styled-components/native'

export const Back = styled.TouchableHighlight`
  z-index: 1;
  margin: 24px;
  justify-content: center;
  width: 100px;
  border-radius: 8px;
`

export const BackButtonContainer = styled.View`
  z-index: 1;
  padding: 8px;
  align-items: center;
  justify-content: center;
  flex-direction: row;
  gap: 8px;
`
