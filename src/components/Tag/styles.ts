import styled from 'styled-components/native'

export const TagContainer = styled.View`
  padding: 4px 8px;
  border-radius: 4px;
  background-color: #effafb;
  opacity: 0.7;
  border: 1px solid rgba(22, 110, 131, 0.5);
`
